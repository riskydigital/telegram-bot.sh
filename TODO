#TODO
- package.json,setup.py ; installer
- ask token and generate config.sh automatically if no config.
- download JSON.sh if not existent
- class of commands check() -> user, admin, owner
- define user level ( amin,user )

